/* Jenkinsfile (Declarative Pipeline) */
pipeline {
    agent none
    /*{ docker { image 'maven:3.3.3' } }*/
    stages {
        stage('build') {
            steps {
                echo 'Hello Shell 0'
            }
        }
    }
}
